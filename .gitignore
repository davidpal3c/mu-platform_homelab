# Private workspace — planning docs, task specs, build reports, engineering log.
# Tracked in its own repository (homelab_lemurjob_workspace01), never in this one.
_workspace/

# Secrets — never commit credentials, keys, or certificates
secrets/
*.key
*.pem
*.p12
*.pfx
kubeconfig
*.kubeconfig

# Environment files
.env
.env.*
!.env.example

# OS
.DS_Store
**/.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*~
