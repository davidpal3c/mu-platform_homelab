# TASK-0001

Title: Ubuntu Server installation and RAID1 boot configuration

Phase: Phase 0 — Platform Foundation (0A — Hardware + OS baseline)

Status: Complete

Priority: Critical

Dependencies: None


# Goal

Install Ubuntu Server as the base operating system for the Aluna Platform node and configure a resilient RAID1 boot configuration using the two Intel SATA SSDs.

The result must be a **headless, SSH-accessible platform node** with:

• predictable mount layout  
• RAID1 mirrored boot disks  
• stable OS environment for Kubernetes  
• disk layout aligned with platform storage design.


# Why Now

The operating system and disk layout define the **foundation for all subsequent infrastructure work**.

If this layer is incorrect, it becomes difficult to:

• isolate platform workloads  
• maintain disk reliability  
• recover from failures  
• preserve storage tier boundaries.


# Architecture References

Relevant context:

• context/Homelab_Hardware_setup.json  
• context/project-context.json  
• docs/platform-overview.md  

Key design principle:

Boot disks must remain isolated from platform and database IO.

## Post-Completion Naming Note

TASK-0001 is complete and remains the historical as-built record for the boot tier.
Naming is now aligned to the Aluna platform, but this task keeps implementation
truth over cosmetic rewrites:

- Platform umbrella name is **Aluna**.
- Legacy identifiers can remain when they represent live, validated system state.
- Device mapping for this node is authoritative: boot RAID is `sda` + `sdc`;
  backup HDD is `sdb`.


## Context Summary for Builder (TASK-0001)

Hardware:

- Lenovo ThinkStation P510  
- Intel SSD Pro 1500 (180GB)  
- Intel SSD 520 (180GB)  

Boot architecture:

- UEFI boot  
- mdadm RAID1  
- Dual EFI partitions (one on each SSD)  

RAID layout:

- /dev/md0 → /boot  
- /dev/md1 → /  
- /dev/md2 → /var  

Partition scheme (per SSD):

- 512MB EFI System Partition  
- 2GB RAID member for /boot  
- 50GB RAID member for /  
- Remaining space RAID member for /var  

Rationale:

- Boot resilience  
- OS stability  
- Log isolation from root filesystem  
- Preparation for future platform workloads on separate tiers  

Constraints:

- No platform workloads on the OS RAID tier  
- k3s runtime and high-churn data must be moved to the platform NVMe (/platform) in later tasks  


# Hardware Involved

Boot Tier (RAID1 mirror)

Disk A:
Intel SSD Pro 1500 Series
~180GB

Disk B:
Intel SSD 520 Series
~180GB

Both drives must participate in:

mdadm RAID1 mirror.


# Boot Mode

UEFI


# Partition Layout

Each SSD must have identical GPT partition layout.

Disk layout per drive:

Partition 1
EFI System Partition

Size:
512 MiB

Filesystem:
FAT32

Mount:
Primary disk mounted at /boot/efi

RAID:
No RAID


Partition 2
BOOT

Size:
2 GiB

Filesystem:
ext4

RAID:
mdadm RAID1 → /dev/md0

Mount:
/boot


Partition 3
ROOT

Size:
50 GiB

Filesystem:
ext4

RAID:
mdadm RAID1 → /dev/md1

Mount:
/


Partition 4
VAR

Size:
Remaining disk space

Filesystem:
ext4

RAID:
mdadm RAID1 → /dev/md2

Mount:
/var


# Why /var is Separate

/var is isolated so that:

• logs  
• container metadata  
• system services  

cannot fill the root filesystem and brick the OS.


# OS Installation Requirements

Distribution:

Ubuntu Server LTS


Installation mode:

Minimal installation.


During installation:

• create RAID arrays using mdadm  
• assign mountpoints as defined above  
• enable OpenSSH server


User requirements:

Create admin user with sudo access.


# Boot Resilience Requirements

After installation:

GRUB must be installed on both disks.

Example:

grub-install /dev/sda  
grub-install /dev/sdc

EFI partition must be duplicated on both disks.

Future sync strategy:

rsync EFI partition contents periodically.


# Post-Installation Configuration

Required system setup:

System update:

apt update
apt upgrade


Install base tooling:

curl
git
htop
nvme-cli
smartmontools


Enable SMART monitoring.


# Networking Requirements

Server must obtain network via DHCP initially.

Static IP configuration may occur later.


# Security Baseline

SSH must be enabled.

Password login temporarily allowed for setup.

Later tasks will enforce:

• SSH key authentication  
• firewall rules  
• fail2ban (optional)


# Acceptance Criteria

The system is considered correctly installed when:

• server boots successfully  
• RAID arrays show healthy state  
• mount layout matches design  
• SSH login works  
• disks are monitored.


# Verification Steps

Run:

lsblk

Confirm RAID devices exist.


Check RAID status:

cat /proc/mdstat


Confirm mounts:

df -h


Check RAID metadata:

mdadm --detail /dev/md0
mdadm --detail /dev/md1
mdadm --detail /dev/md2


Reboot test:

System must reboot cleanly and remain accessible via SSH.


# Failure Conditions

Task fails if:

• RAID arrays degraded after install  
• mountpoints incorrect  
• boot works only from one disk  
• OS disk usage already near capacity.


# Risks

Mixed-model SSD RAID.

Risk mitigation:

Mirror size determined by smallest drive.


Potential BIOS boot order issue.

Mitigation:

Ensure both disks are bootable.


# Output Required From Builder

The Builder Agent must generate:

Operator Runbook containing:

• step-by-step installation process  
• mdadm RAID creation commands  
• partition commands  
• GRUB configuration  
• EFI sync instructions  
• verification checklist.


# Documentation That Must Be Updated

After completion the following must be updated:

docs/system-blueprint.md  
docs/platform-overview.md  
context/project-context.json  

A build report must be created:

docs/build-reports/TASK-0001.md


# Next Task

Upon successful verification the next task becomes:

TASK-0002  
Storage tier mount layout.