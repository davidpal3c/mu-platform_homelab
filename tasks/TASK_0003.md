# TASK-0003 — k3s Cluster Bootstrap (Phase 0C)

**Status:** Ready  
**Phase:** 0 — Platform Foundation / **0C — Kubernetes Platform Bootstrap**  
**Dependencies:** TASK-0002 complete and verified

**Canonical task spec:** this file. `tasks/todo.md` carries an index entry only.

---

## Lessons to apply (from `tasks/lessons.md`)

Surface these in the operator runbook preamble and acceptance checks where relevant:

| ID | Use in TASK-0003 |
|----|------------------|
| **INF-002** | Document actual block-device / mount mapping at top of any step touching paths; no assumed `sdX` roles beyond `project-context.json`. |
| **INF-003** | k3s does not re-partition disks; still state **in-scope paths** (`/platform` binds, `/data`, `/backups`) vs **out-of-scope** (`sda`, `sdc`, `md*`) if any disk prep is mentioned. |
| **INF-005** | Explicit rollback: k3s uninstall path + confirm RAID and tier mounts unchanged. |
| **LINUX-003** | If `fstab` is touched for k3s-related reasons, preserve base-before-bind order (usually unchanged post TASK-0002). |
| **VER-001** | Validate with `kubectl`, `findmnt`, `lsblk` — not assumptions. |
| **VER-002** | If verifying storage, match `blkid` / `findmnt` sources to `fstab` UUID lines (post TASK-0002 layout). |
| **SEC-001** | Review k3s defaults (API bind, firewall, servicelb); no public ingress or workload exposure in this task. |
| **INF-007** | TASK-0003 must not `mkfs` or repartition tiers; if any step touches block devices, **stop** and re-verify with `lsblk -o NAME,SIZE,MODEL` (backup = `sdb`, RAID second disk = `sdc`). |

---

## Objective

Install and stabilize a single-node k3s control plane on `aluna-node-01` while preserving storage-tier separation established in Phase 0B.

---

## Why now

Phase 0A and 0B are complete. The node has:

- RAID1 boot tier in service (`/boot`, `/`, `/var`)
- `/platform`, `/data`, `/backups` mounted via UUID
- bind mounts from `/platform/*` to `/var/lib/*` high-churn runtime paths

This is the correct point to bootstrap Kubernetes without violating storage architecture principles.

---

## Scope

- Install k3s control plane on the host.
- Validate that runtime data paths used by k3s resolve to `/platform` via bind mounts.
- Configure baseline namespaces:
  - `ceiba`
  - `aluna-context`
  - `aluna-mira`
  - `aluna-terra`
  - `observability`
- Verify cluster readiness, networking baseline, and DNS.
- Verify default storage class and run a PVC smoke test using local-path.
- Produce a **step-by-step operator runbook** (Builder guides operator; operator executes on the node).

---

## Naming convention decision (locked for execution)

For this task, namespace and runtime naming uses:

- `ceiba` — Kubernetes namespace for the **Ceiba** control-plane product (Aluna Control domain)
- `aluna-context`
- `aluna-mira`
- `aluna-terra`
- `observability`

Do not introduce new `opencontext-*` namespaces in TASK-0003.
Any legacy-name migration beyond this scope is handled by a later, explicit
naming convergence task.

---

## Non-scope

- TLS and public ingress hardening (TASK-0005)
- Observability stack deployment (TASK-0004)
- Aluna Context API or any workload deployment
- Multi-node clustering or HA control-plane design

---

## Constraints

- Do not alter boot RAID layout (`sda`/`sdc`, `md0/md1/md2`).
- Do not reformat storage tiers.
- Keep runtime churn off boot tier by honoring existing bind mounts.
- Maintain infrastructure-only focus for Phase 0.

---

## Risks

- k3s may start with incorrect runtime path assumptions if bind mounts are absent at boot.
- Single-node capacity constraints (CPU/memory/disk) may affect control-plane stability.
- Networking/firewall defaults can block API server access or pod networking.

---

## Acceptance criteria

1. `kubectl get nodes` reports node `Ready`.
2. `kubectl get pods -A` shows core system pods healthy.
3. Required namespaces exist.
4. `findmnt` confirms `/var/lib/rancher`, `/var/lib/kubelet`, `/var/lib/containerd` are backed by `/platform/*`.
5. A smoke-test PVC binds and a test pod mounts it successfully.
6. Operator confirms all acceptance criteria on the live cluster; **then** Builder drafts `docs/build-reports/BR-TASK-0003.md` for review (no build report before that confirmation).

---

## Verification checklist

- [ ] `kubectl get nodes -o wide`
- [ ] `kubectl get pods -A -o wide`
- [ ] `kubectl get ns`
- [ ] `findmnt /var/lib/rancher /var/lib/kubelet /var/lib/containerd`
- [ ] `kubectl get sc`
- [ ] `kubectl get pvc,pv -A` after smoke test
- [ ] `cat /proc/mdstat` (confirm RAID unchanged)

---

## Builder deliverable

**During execution:** guide the operator one step at a time; wait for command output / confirmation between steps. **Do not** write `docs/build-reports/BR-TASK-0003.md` until the operator explicitly confirms TASK-0003 acceptance criteria are met on `aluna-node-01`.

**After operator confirmation:** produce `docs/build-reports/BR-TASK-0003.md` (commands used, validation, risks, rollback) for Documentation / Reviewer handoff.

Operator runbook content (delivered incrementally or as a single draft at end, per session agreement) must cover:

- prereq checks (`lsblk -f`, bind mounts, swap status, required packages)
- k3s install command and configuration
- kubeconfig access steps
- namespace bootstrap commands (`ceiba`, `aluna-context`, `aluna-mira`, `aluna-terra`, `observability` only — no new `opencontext-*`)
- PVC smoke test manifest + verify commands
- rollback notes (k3s uninstall + validation of mounts and RAID unaffected)

---

## Documentation after verify

Update:

- `docs/system-blueprint.md`
- `docs/platform-overview.md`
- `context/project-context.json`
- `tasks/todo.md` (status transition to Complete after verification + docs sync)

---

## Next task

TASK-0004 — Observability stack baseline (Phase 1) once TASK-0003 is accepted.
